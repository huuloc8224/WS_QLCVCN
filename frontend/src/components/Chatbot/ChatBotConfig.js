import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "Trợ lý Công việc",
  initialMessages: [createChatBotMessage("Xin chào! Tôi có thể giúp gì cho bạn?")],
};

export default config;
